interface DiagnosisIntroProps { hasSavedProgress: boolean; onStart: () => void; onResume: () => void; onRestart: () => void; onBack: () => void; }

export function DiagnosisIntro({ hasSavedProgress, onStart, onResume, onRestart, onBack }: DiagnosisIntroProps) {
  return <section className="screen active" id="intro"><div className="shell"><header className="topbar"><div className="brand"><span className="brand-mark"/>INNER NOTE</div><button className="linkbtn" onClick={onBack}>トップへ戻る</button></header><main className="center-main"><section className="panel"><div className="panel-head"><div className="kicker">Before you begin</div><h2>今のあなたに近いものを、<br/>直感で選んでください。</h2><p>理想の自分ではなく、普段の自分に近い回答を選んでください。</p></div><div className="info-grid"><Info title="約10分前後" text="回答によって質問数が変わります。"/><Info title="直感で回答" text="正解・不正解はありません。"/><Info title="途中から再開" text="同じブラウザから続きに戻れます。"/></div>{hasSavedProgress && <div className="status-box"><strong>前回の診断が途中で保存されています。</strong><div>続きから再開するか、保存済み回答を破棄して最初から始められます。</div></div>}<div className="panel-actions">{hasSavedProgress ? <><button className="secondary" onClick={onBack}>トップへ戻る</button><button className="secondary" onClick={onRestart}>最初からやり直す</button><button className="primary" onClick={onResume}>診断を続ける</button><button className="ghost" onClick={onStart}>新しく診断を開始する</button></> : <><button className="secondary" onClick={onBack}>トップへ戻る</button><button className="primary" onClick={onStart}>診断を始める</button></>}</div></section></main></div></section>;
}

function Info({ title, text }: { title: string; text: string }) { return <article className="info-item"><h3>{title}</h3><p>{text}</p></article>; }
